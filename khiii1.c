#include <stdio.h>

/* binsearch; find x in v[0] <= v[1] <= ... <= v[n-1] and return its index, return -1 if not in the list */
int binsearch(int x, int v[], int n)
{
	int low, mid, high;
	low = 0;
	high = n-1;
	while (low <= high) {

	}
}